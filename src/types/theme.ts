// Theme mode
export type ThemeMode = "light" | "dark"

// Component size variants (used across buttons, inputs, etc.)
export type SizeVariant = "sm" | "md" | "lg"

// Semantic color intents
export type ColorIntent = "primary" | "secondary" | "success" | "warning" | "error" | "neutral"

// Elevation levels
export type ElevationLevel = 0 | 1 | 2 | 3 | 4 | 5

// Responsive breakpoint keys
export type Breakpoint = "sm" | "md" | "lg" | "xl" | "2xl"

// Button variants as defined in the design system JSON
export type ButtonVariant = "filled" | "outlined" | "text" | "tonal"

// Table density variants
export type TableDensity = "compact" | "comfortable"

// Dialog size variants
export type DialogSize = "sm" | "md" | "lg" | "xl"
